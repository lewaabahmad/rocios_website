source 'https://rubygems.org'

gem 'pry'
gem 'pry-nav'

gem 'rails', '4.2.0'
gem 'pg'
gem 'sass-rails', '5.0.3'
gem 'uglifier', '2.7.1'
gem 'coffee-rails', '4.1.0'

gem 'jquery-rails', '4.0.3'
gem 'turbolinks', '2.5.3'
gem 'jbuilder', '2.2'
gem 'sdoc', '~> 0.4.0', group: :doc

group :development, :test do
  gem 'byebug', '4.0.5'
  gem 'web-console', '2.1.2'
  gem 'spring'
end

group :test do
  gem 'minitest-reporters', '1.0.5' # colors
  gem 'mini_backtrace', '0.1.3' # remove useless trace after test fails 
  gem 'guard-minitest', '2.3.1' # avoids need to keep running rake command to run test; automatically watches for changes to run tests
end

# Bootstrap 
gem 'bootstrap-sass', '3.3.4.1'
gem 'autoprefixer-rails', '5.2'
